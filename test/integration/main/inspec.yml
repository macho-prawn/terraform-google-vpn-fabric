name: gcp-profile
title: GCP InSpec Profile
summary: An InSpec Compliance Profile For GCP
version: 0.1.0
inspec_version: '>= 2.3.5'

# Declare the inputs needed by test cases
inputs:
- name: project_id
  required: true
  description: 'The GCP project identifier.'
  type: string

Comment: >
    Declare any other remaining inputs, For example below is the section to declare a bucket name 
    - name: bucket_name
    required: true
    description: 'The GCP project identifier.'
    type: string

depends:
- name: inspec-gcp
  url: https://github.com/inspec/inspec-gcp/archive/v1.8.0.zip

supports:
- platform: gcp
